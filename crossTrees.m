%% Cross Trees
function newTree = crossTrees(tree1, tree2, maxSize)
    % random select node on tree 1
    
    % cut out node of tree 1
    
    % random select node of tree 2 (limited by tree 1 node depth + )
    
    
end