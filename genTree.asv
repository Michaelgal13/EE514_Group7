%%
function treeOut = genTree(depth)
if depth <= 0
    treeOut = 0;
else if depth == 1
     treeOut = tree(randOperator(0.5));
     else
       treeOut = tree(randOperator(0.5)); 
        for i = 2:depth
            for j = 1
            treeOut = treeOut.addnode(i/2, )
        end
end
end
